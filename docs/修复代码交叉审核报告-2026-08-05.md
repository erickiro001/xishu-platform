# 修复代码交叉审核报告

> 审核日期：2026-08-05
> 审核对象：远程提交 `d435d14`（作者 Heyana，2026-08-05 13:13）—— 删除 reorder 请求中的硬编码 `company_id: 1`
> 审核方式：commit diff 逐行审查 + 全项目同类残留扫描 + 类型检查验收

---

## 一、审核结论

**修复正确、完整、无副作用，验收通过 ✅**

| 维度 | 结果 |
|------|:---:|
| 修复正确性 | ✅ |
| 同类残留 | ✅ 无 |
| 类型检查 | ✅ 通过（vue-tsc -b --noEmit, exit 0） |
| 范围控制 | ✅ 仅改目标两行 |

## 二、修复内容核验（commit d435d14）

```diff
# admin-frontend/src/pages/demands/DemandList.tsx (doReorder)
- await api.put('/admin/demands/reorder', { company_id: 1, ids: list.map((d) => d.id) })
+ await api.put('/admin/demands/reorder', { ids: list.map((d) => d.id) })

# admin-frontend/src/pages/news/NewsList.tsx (doReorder)
- await api.put('/admin/news/reorder', { company_id: 1, ids: list.map((item) => item.id) })
+ await api.put('/admin/news/reorder', { ids: list.map((item) => item.id) })
```

- 与审计报告 P1-6 描述一致（`company_id: 1` 为复制残留）。
- 请求体已收敛为 `{ ids }`，与代码审查报告所述后端契约（`ReorderNews(req.IDs)` 只收 ids）一致。

## 三、同类残留扫描（全 admin-frontend/src）

`grep -rn "company_id" admin-frontend/src/` 结果：
- `CompanyEditor.tsx:144/183/244/283` —— **业务必需**：方案(solutions)/案例(cases) 的 reorder 与 body 使用 `company_id: companyId.value`（当前编辑企业 ID，动态值，非硬编码），属企业维度数据隔离的正确用法，**不需要修改**。

**结论：无硬编码残留**，修复覆盖完整。

## 四、范围确认

远程自上次拉取（f7e6bdf 之后）共有 2 个新提交：
- `5832f08`：审计产物（报告 + `.codebase-memory/`、`.hotplate/logs/` 工具数据）
- `d435d14`：本次审核的修复（company_id ×2）+ 修复计划文档

**除 company_id 修复外，其余审计项（kimi 插件、MemoryRouter、wangeditor pasteImage、视频压缩、列表字段裁剪等）均未变更**，仍待后续按《修复计划》执行。

## 五、遗留提醒

1. 该修复无 CI/构建记录（Heyana 提交未附验证），本机已补跑 `vue-tsc -b --noEmit` 通过，可放心。
2. 后端 `reorder` 契约（仅收 ids）建议最终与后端确认一次（后端仓库不在本工作区）。
3. 提交信息为 "1"，建议后续统一提交信息规范。
