# 修复计划交叉审核报告

> 审核日期：2026-08-05
> 审核对象：`docs/plans/2026-08-05-fix-frontend.md`（前端 7 项 + 后端 2 项修复计划）
> 审核方式：逐任务对照实际代码核验 + 公网接口实测 + git 历史比对

---

## 一、总体结论

**计划整体质量良好**：目标明确、任务拆解清晰、每任务含代码级 diff 与验收步骤，关键前提（详情接口存在、列表含 content、视频体积）**经实测全部成立**。但存在 3 处需要执行者注意的偏差：

1. **Task 1（company_id 删除）已被远程代码完成**（commit `d435d14`，作者 Heyana，13:13），执行时跳过或仅验证；
2. **计划内所有路径为 Windows 格式（`E:/hxy/project-2026/...`）**，本机为 macOS，执行时需替换为实际路径；
3. **Task 7 依赖 `xishu_online` 后端仓库，当前工作区不存在**，需后端同事或在后端仓库环境执行。

---

## 二、前提实测验证（全部通过 ✅）

| 计划前提 | 实测结果 | 状态 |
|---------|---------|:---:|
| `GET /api/v1/news/:id` 详情接口存在且公网可用 | `GET /api/v1/news/31` → **HTTP 200**，返回完整 content | ✅ |
| 列表接口含 content 字段（Task 7 裁剪目标） | 列表元素 keys 含 `content`；**page_size=2 响应即 2.1MB**（单条约 1MB） | ✅ |
| 首页视频 40.6MB | `前端/public/assets/video/video.mp4` 实测 **39MB** | ✅ |
| poster 800KB | `cover.png` 实测 **786KB** | ✅ |

## 三、逐任务核验

### Task 1 · reorder 删除 company_id —— ⚠️ 已完成，跳过
- **代码位置核验**：`git log` 显示远程提交 `d435d14`（2026-08-05 13:13）已修改 `NewsList.tsx` / `DemandList.tsx`（各 1 行）。
- **当前代码确认**：
  - `NewsList.tsx` doReorder：`await api.put('/admin/news/reorder', { ids: ... })` ✅
  - `DemandList.tsx` doReorder：`await api.put('/admin/demands/reorder', { ids: ... })` ✅
- 该修复与本计划（Task 1）是**同一提交**产出，计划文本未勾选 checkbox 属时间差。**结论：无需再执行**；后端契约（reorder 只收 ids）建议顺带向后端确认一次。

### Task 2 · kimi 插件仅开发模式 —— ✅ 可执行
- `前端/vite.config.ts:6`：`import { inspectAttr } from 'kimi-plugin-inspect-react'` + `plugins: [inspectAttr(), react()]`，**无环境判断**，问题真实。
- 计划方案可行；`process.env.NODE_ENV` 在 vite.config 中可用。更 Vite 惯用的写法：`defineConfig(({ mode }) => mode === 'development' ? [inspectAttr(), react()] : [react()])`，两者皆可。

### Task 3 · MemoryRouter → HashRouter —— ✅ 可执行，但需前置确认
- `前端/src/main.tsx`：`import { MemoryRouter } from 'react-router'`（第 3 行）+ `<MemoryRouter>`（第 30 行），位置准确。
- **⚠️ 前置确认（计划未写明）**：main.tsx 含 `applySafeAreaFix()` 微信小程序/企微容器适配。若门户端**仅内嵌小程序 WebView**，MemoryRouter 是合理设计，改 HashRouter 反而无意义；若已作为**独立公网站点**部署（`.env.production` 直连公网 API + `base: './'`），则改 HashRouter 正确。**执行前必须先确认部署形态**。
- 补充：改 HashRouter 后 `base: './'` 兼容，无需改构建配置。

### Task 4 · 详情页直连接口 —— ✅ 可执行（前提已实测成立）
- `前端/src/lib/services.ts` fetchArticle 当前确为**循环翻页全量查找**（注释"后端无单条公开详情接口"已过时）。
- 计划重写为直连 `/api/v1/news/:id`，与实测接口一致；`mapArticleDetail` 已存在。签名不变，消费方无感知。**预期收益：详情页从 ~26MB 降到 ~1MB**。
- 建议：new 代码中补 404 兜底（raw 为 null 时返回 null，详情页显示"未找到"），计划已含该逻辑。

### Task 5 · 首页取前 6 条 + PAGE_SIZE 12 —— ✅ 可执行
- `HomePage.tsx:167`：`useFetch(fetchArticles)` 与计划 old 一致。
- `useFetch` 签名核验：`useFetch<T>(fetcher: (signal) => Promise<T>, deps = [])`，计划 new 写法 `useFetch((signal) => fetchArticlesPage(1, 6, signal).then((r) => r.list), [])` **签名兼容** ✅。
- `fetchArticles` 引用核验：仅 `services.ts:295`（定义）+ `HomePage.tsx:3`（import）+ `:167`（使用），改造后可安全移除 HomePage 引用。
- `AllNewsPage.tsx:9`：`const PAGE_SIZE = 20` 与计划一致。
- 小提醒：`PAGE_SIZE = 12` 为纯展示调优，无正确性问题，可按产品偏好决定 12/20。

### Task 6 · wangeditor 粘贴图片拦截 —— ✅ 可执行（风险已标注）
- `admin-frontend/src/components/RichEditor.tsx`：当前 `editorConfig` 仅配置 `uploadImage.customUpload`，**无 pasteImage 拦截、无 base64LimitSize**，问题真实。
- 计划已注明 wangeditor v5 `customPaste` 类型可能不支持的回退方案。补充：`base64LimitSize: 0` 为顶层 editorConfig 配置（非 MENU_CONF），计划代码中位置正确。
- 风险等级：中（改后需手工验证粘贴/拖拽图片两种路径）。

### Task 7 · 后端列表裁剪 + GetNews 校验 —— ⚠️ 需后端环境，本工作区无法核验
- **当前工作区没有 `xishu_online` 后端仓库**（只有 `admin-frontend` / `前端` / `docs`），`business_service.go:148-161` 等行号无法核验。
- 前提实测成立：列表接口确实全量返回 content（page_size=2 → 2.1MB）。
- 计划 Step 2 的自我修正（管理端详情也走 GetNews，故新增 `GetPublishedNews` 而非改原方法）**判断正确**，是本次审核认可的设计。
- 建议：由持有后端仓库的执行者在后端环境执行，行号以实际代码为准。

### Task 8 · 视频压缩 + 懒加载 —— ✅ 可执行，两处补充
- 文件实测 39MB + poster 786KB，问题真实。
- ⚠️ **`-an`（去音轨）需确认**：若视频含业务音效/配音，去音轨会静音；建议先 `ffprobe` 确认音轨再决定。
- 补充遗漏：poster 786KB 未在计划中压缩（审查报告 P0-2 提过 poster 800KB），建议一并压到 ~100KB 或转 WebP。

### Task 9 · 全量验收 —— ✅ 合理
- `grep company_id: 1` 与 `page_size: 10000` 的残留检查正确；后者建议同步查 `前端/src/lib/services.ts:245`（fetchDemands 全量拉取，P2-10 未在计划内）。

## 四、覆盖度对照（审计 14 项 vs 计划 9 任务）

| 审计项 | 计划任务 | 状态 |
|-------|---------|:---:|
| P0-1 MemoryRouter | Task 3 | ✅ 覆盖（需先确认部署形态） |
| P0-2 首页视频 40MB | Task 8 | ✅ 覆盖（poster 未含，见上） |
| P0-3 列表全量返回 | Task 7 | ✅ 覆盖（需后端环境） |
| P1-4 kimi 插件 | Task 2 | ✅ 覆盖 |
| P1-5 管理端分页 | **未覆盖** | ⚠️ 计划 Global Constraints 明确"不动管理端分页交互"，属**有意取舍**（拖拽排序依赖全量列表），合理但建议在计划中记录"接受现状"结论 |
| P1-6 company_id | Task 1 | ✅ **已被远程提交完成** |
| P2-7 wangeditor base64 | Task 6 | ✅ 覆盖 |
| P2-8 Dashboard 200 条 | **未覆盖** | 可后续处理 |
| P2-9 登录硬编码用户 | **未覆盖** | 可后续处理（`Login.tsx:25` `id: 0`） |
| P2-10 门户全量拉需求/HTML 误判 | **部分覆盖** | 新闻侧由 Task 4/5 处理；`fetchDemands` 全量与 `isHtmlContent` 误判未处理 |
| P3-11/12 硬编码颜色 | 未覆盖 | 低优先级，按需 |
| P3-13 shadcn 死代码 | 未覆盖 | 低优先级，按需 |
| P3-14 附件 100MB | 未覆盖 | 低优先级 |

## 五、其他发现（计划外，建议关注）

1. **`.codebase-memory/`、`.hotplate/logs/` 工具产物已入库**（远程提交 5832f08），建议从仓库移除并加入 `.gitignore`，避免持续污染。
2. **提交信息质量为"1"**（两个远程提交），建议后续统一提交信息规范（如 `fix(admin): ...`）。
3. 计划中 `Spec: .workbuddy/specs/2026-08-05-fix-frontend/SPEC.md` 引用——本工作区 `.workbuddy/specs/` 下无此文件（`.workbuddy` 已 gitignore），执行时以本计划为准。

## 六、执行建议

**建议执行顺序**（跳过 Task 1）：
1. Task 3 前置确认部署形态（小程序内嵌 vs 独立站点）→ 未确认前先跳过
2. Task 2 → Task 6（管理端，可先做）
3. Task 4 → Task 5 → Task 8（门户端，需 yarn build 验证）
4. Task 7（后端，在 xishu_online 仓库执行，完成后做 Task 9 公网验证）

执行者注意：所有 `E:/hxy/project-2026/` 路径替换为本机实际路径；两端 build 用 yarn；后端 go build。
